import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

// TODO

// test('renders specific string', () => {
//   render(<App />);
//   const linkElement = screen.getByText(/Matching Memory Game/i);
//   expect(linkElement).toBeInTheDocument();
// });
